 BufferReader br = new BufferReader("../student/Etudiant.class")

 //if !br.lines().anyMatch(s -> s.trim().contains("BufferedReader"))) { fail }

 // si close in try mais pas in catch => error
 //if !br.lines().anyMatch(s -> s.trim().contains("close")));

 // check close in catch / (en fait non ça check si null donc avec br.ready() ça passe aussi)
 @Test(expected = NullPointerException.class)
    public void test() throws NullPointerException {

    	int count = 0;
        try{
        count = Etudiant.countLines("./fileFAKE");
        } catch(IOException e){}
        catch(ConstructException e){}
        System.err.println("Vous ne fermez pas le buffer lors d'une IOException");
    }

    // check si -1
       @Test
    public void testAnatole() {
    	int count = 0;
        try{
        count = Etudiant.countLines("./fileFAKE");
        } catch(Exception e){}
        //System.err.println(count);
        assertTrue("Vous ne retournez pas -1a", count == -1);
    }